# Copy this file to .env and fill in real values, then either:
#   - export each variable yourself before running `python app.py`, or
#   - use a tool like python-dotenv if you prefer to load it automatically.

# Admin dashboard password (used as ?pw=... in the /admin URL)
ADMIN_PASSWORD=muhindo2026

# --- Optional: email notifications when someone RSVPs ---
# Leave SMTP_HOST empty to disable email entirely.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jmussamuhindo@gmail.com
SMTP_PASS=your_gmail_app_password_here
NOTIFY_EMAIL=jmussamuhindo@gmail.com
